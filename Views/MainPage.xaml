<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="or1n_rename_file_name_to_date_created.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:or1n_rename_file_name_to_date_created"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">
    <Page.Content>
        <Grid Background="{StaticResource WindowBackgroundBrush}" Padding="8" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Margin="0,4,0,2" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Style="{StaticResource MyLabel}" Text="or1n Rename File Name To Date Created" />

            <!-- Short Description -->
            <TextBlock Grid.Row="1" Margin="0,0,0,4" FontSize="16" HorizontalAlignment="Center" TextWrapping="Wrap" Text="A modern WinUI 3 app to batch rename files in a selected folder to their original date created/taken, with customizable formatting and smart options." />

            <!-- Folder Picker and Scan Button -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4" Padding="0" Margin="0">
                <Button x:Name="OpenFolderButton" Content="Open Folder" Click="OpenFolderButton_Click" />
                <TextBlock x:Name="SelectedFolderText" VerticalAlignment="Center" FontSize="14" Margin="4,0,0,0" Text="No folder selected" />
                <Button x:Name="ScanButton" Content="Scan" Click="ScanButton_Click" />
            </StackPanel>

            <!-- Log/Terminal Area -->
            <Border Grid.Row="3" Margin="0,2,0,0" Background="{StaticResource LogBackgroundBrush}" CornerRadius="6" Padding="0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0" Margin="0">
                    <TextBlock x:Name="InfoTextBlock" Margin="2,2,2,2" FontSize="13" Foreground="{StaticResource LogForegroundBrush}" Text="Ready." TextWrapping="Wrap"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Page.Content>
</Page>
